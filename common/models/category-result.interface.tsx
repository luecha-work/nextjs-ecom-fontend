export interface categoryResult {
  id: string;
  categoryName: string;
  categoryCode: string;
  categoryDetail: string;
  createAt: Date;
  updateAt: Date;
  createBy: string;
  updateBy: string;
  active: boolean;
}
